# Always install devDependencies, even when NODE_ENV=production (e.g. on Render).
# The production build runs `vite build`, and vite is a devDependency — without
# this, `npm install` under NODE_ENV=production omits it and the build fails with
# "vite: not found". npm's `include` always wins over the production-mode omit.
include=dev
